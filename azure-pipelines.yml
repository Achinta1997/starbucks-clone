trigger: none

stages:
  - stage: deploy
    jobs:
      - deployment: starbucksDeploy
        pool: 
         name: vmpool
        environment:
         name: dev
        strategy: 
         runOnce:
           deploy:
             steps:
             - task: CmdLine@2
               inputs:
                 script: 'sudo mkdir -p /app'